# 🚀 Quick Start Guide

## การติดตั้งและรัน

### 1. ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

### 2. รันแอปพลิเคชัน

**Windows:**
```bash
start.bat
```

**หรือ:**
```bash
python run.py
```

**Linux/Mac:**
```bash
python3 run.py
```

### 3. เปิดเบราว์เซอร์

```
http://localhost:5000
```

## วิธีใช้งานเบื้องต้น

### ขั้นตอนที่ 1: ค้นหาสถานที่

1. พิมพ์ชื่อสถานที่ในช่องค้นหา
   - ตัวอย่าง: "Central World", "Siam Paragon", "Terminal 21"
2. กด Enter หรือคลิกปุ่ม "ค้นหา"
3. รอจนแสดงผลลัพธ์

### ขั้นตอนที่ 2: เลือกสถานที่

1. คลิกที่การ์ดสถานที่ที่ต้องการ scrape
2. ตรวจสอบข้อมูล:
   - ชื่อสถานที่
   - Rating และจำนวนรีวิว
   - ที่อยู่
   - Place ID

### ขั้นตอนที่ 3: ตั้งค่า (Optional)

1. คลิกปุ่ม "⚙️ ตั้งค่า" ด้านบนขวา
2. ปรับแต่งตามต้องการ:
   - **จำนวนรีวิว**: 1-1000 (default: 100)
   - **ช่วงเวลา**: 1 เดือน - ทั้งหมด
   - **ภาษา**: ไทย, English, 日本語, 中文
3. คลิก "บันทึก"

### ขั้นตอนที่ 4: เริ่ม Scraping

1. คลิกปุ่ม "เริ่ม Scraping"
2. ระบบจะสลับไปหน้า "ความคืบหน้า" อัตโนมัติ
3. ดูความคืบหน้าแบบ real-time:
   - Progress bar แสดง %
   - จำนวนรีวิวที่ดึงได้
   - Status (กำลังทำงาน/เสร็จสิ้น)

### ขั้นตอนที่ 5: ดูผลลัพธ์

1. รอจนสถานะเป็น "เสร็จสิ้น"
2. ไฟล์จะถูกบันทึกใน folder `outputs/`
3. รูปแบบไฟล์:
   - `reviews.json` - รีวิวทั้งหมด (JSON)
   - `reviews.csv` - รีวิวทั้งหมด (CSV)
   - `metadata.json` - ข้อมูล metadata
   - `settings.json` - การตั้งค่าที่ใช้

## ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: Scrape รีวิว Central World

1. ค้นหา "Central World"
2. เลือกผลลัพธ์แรก
3. ตั้งค่า: 100 รีวิว, 1 ปีล่าสุด
4. คลิก "เริ่ม Scraping"
5. รอ 2-3 นาที
6. ดูผลลัพธ์ใน `outputs/`

### ตัวอย่างที่ 2: Scrape รีวิวหลายร้าน

1. ค้นหา "Siam"
2. เลือก Siam Paragon
3. Scrape (100 รีวิว)
4. รอให้เสร็จ
5. กลับไปหน้าค้นหา
6. เลือก Siam Center
7. Scrape อีกครั้ง

## Features หลัก

### 🔍 ค้นหาสถานที่
- ค้นหาชื่อสถานที่ภาษาไทย/อังกฤษ
- แสดง Place ID จริงที่ใช้งานได้
- แสดง rating และจำนวนรีวิว

### 📊 ติดตามความคืบหน้า
- Real-time progress updates
- Progress bar แสดง %
- นับจำนวนรีวิวที่ดึงได้

### ⚙️ การตั้งค่า
- จำนวนรีวิวต่อร้าน (1-1000)
- ช่วงเวลา (1 เดือน - ทั้งหมด)
- ภาษาและพื้นที่
- บันทึกอัตโนมัติใน localStorage

### 💾 Export ผลลัพธ์
- JSON format (รายละเอียดครบถ้วน)
- CSV format (นำเข้า Excel ได้)
- Metadata และ settings

## ข้อควรทราบ

### ⚡ Performance
- ประมาณ 40+ รีวิว/วินาที
- 100 รีวิว ใช้เวลา ~2-3 นาที
- มี anti-bot protection อัตโนมัติ

### 🔒 Limitations
- Task data เก็บใน memory (หายเมื่อ restart)
- Single-server only
- No authentication (เพิ่มก่อน production)

### 🐛 Troubleshooting

**Port 5000 ถูกใช้งานแล้ว:**
```python
# แก้ไขใน app.py หรือ run.py
app.run(port=5001)
```

**Import error:**
```bash
# ตรวจสอบว่าติดตั้ง dependencies แล้ว
pip install -r requirements.txt
```

**Encoding error (Windows):**
- Script จัดการอัตโนมัติผ่าน `chcp 65001`
- ถ้ายังมีปัญหา ลอง run เป็น admin

## Next Steps

✅ ระบบพร้อมใช้งาน!

ลองเริ่มจาก:
1. ค้นหา "Central World"
2. Scrape 50 รีวิว
3. ดูผลลัพธ์ใน `outputs/`

สนุกกับการ scrape! 🚀
