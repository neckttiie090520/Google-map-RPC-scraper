# 🚀 Quick Start Guide
## Google Maps RPC Scraper - Perfect System

**เวอร์ชัน:** 2.0 Perfect Edition
**วันที่:** 2025-11-10

---

## ⚡ การเริ่มใช้งานด่วน

### ขั้นตอนที่ 1: เริ่ม Backend Server

```bash
cd webapp
python api_v2.py
```

**คุณจะเห็น:**
```
======================================================================
PERFECT BACKEND API v2 - GOOGLE MAPS SCRAPER
======================================================================

Features:
  [OK] Real-time progress tracking (page-by-page)
  [OK] Detailed logging with timestamps
  [OK] Complete stats monitoring
  [OK] SSE streaming for instant updates
  [OK] Proper error handling

Starting Flask server...
API v2 Endpoint: http://localhost:5001
```

✅ **Server พร้อมใช้งานที่:** `http://localhost:5001`

---

### ขั้นตอนที่ 2: ค้นหาสถานที่

**Windows PowerShell:**
```powershell
$body = @{
    query = "ข้าวซอยนิมมาน เชียงใหม่"
    max_results = 5
    language = "th"
    region = "th"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:5001/api/v2/search" `
    -Method Post `
    -Body $body `
    -ContentType "application/json"
```

**Linux/Mac:**
```bash
curl -X POST http://localhost:5001/api/v2/search \
  -H "Content-Type: application/json" \
  -d '{
    "query": "ข้าวซอยนิมมาน เชียงใหม่",
    "max_results": 5,
    "language": "th",
    "region": "th"
  }'
```

**ผลลัพธ์:**
```json
{
  "success": true,
  "count": 5,
  "places": [
    {
      "place_id": "0x30da3a9a7e41e7e3:0x9ecd2c1918c6d746",
      "name": "ข้าวซอยนิมมาน",
      "address": "...",
      "rating": 4.5,
      "total_reviews": 1234
    }
  ]
}
```

---

### ขั้นตอนที่ 3: เริ่มดึงข้อมูล (Scraping)

**สร้างไฟล์ `scrape_request.json`:**
```json
{
  "places": [
    {
      "place_id": "0x30da3a9a7e41e7e3:0x9ecd2c1918c6d746",
      "name": "ข้าวซอยนิมมาน",
      "address": "...",
      "rating": 4.5,
      "total_reviews": 1234
    }
  ],
  "settings": {
    "max_reviews": 100,
    "language": "th",
    "region": "th",
    "date_range": "1year",
    "fast_mode": true
  }
}
```

**Windows PowerShell:**
```powershell
$body = Get-Content scrape_request.json | ConvertFrom-Json | ConvertTo-Json -Depth 10
Invoke-RestMethod -Uri "http://localhost:5001/api/v2/scrape" `
    -Method Post `
    -Body $body `
    -ContentType "application/json"
```

**Linux/Mac:**
```bash
curl -X POST http://localhost:5001/api/v2/scrape \
  -H "Content-Type: application/json" \
  -d @scrape_request.json
```

**ผลลัพธ์:**
```json
{
  "success": true,
  "task_id": "20251110_235959_abc12345",
  "message": "Scraping task started",
  "places_count": 1
}
```

✅ **บันทึก `task_id` นี้ไว้!**

---

### ขั้นตอนที่ 4: ติดตามความคืบหน้า

**แบบที่ 1: ตรวจสอบสถานะ (Get Status)**

```bash
curl http://localhost:5001/api/v2/tasks/20251110_235959_abc12345
```

**ผลลัพธ์:**
```json
{
  "success": true,
  "task": {
    "task_id": "20251110_235959_abc12345",
    "status": "running",
    "progress": {
      "current_place_index": 0,
      "current_place_name": "ข้าวซอยนิมมาน",
      "current_page": 5,
      "reviews_scraped_current": 87,
      "reviews_scraped_total": 87,
      "scraping_rate": 28.5,
      "successful_requests": 5,
      "failed_requests": 0
    },
    "logs": [
      {
        "timestamp": "2025-11-10T23:59:59",
        "level": "info",
        "message": "Starting place 1/1: ข้าวซอยนิมมาน"
      },
      {
        "timestamp": "2025-11-11T00:00:15",
        "level": "debug",
        "message": "Page 5: 87 reviews scraped"
      }
    ]
  }
}
```

**แบบที่ 2: Real-time SSE Stream (แนะนำ)**

```bash
curl -N http://localhost:5001/api/v2/tasks/20251110_235959_abc12345/stream
```

**คุณจะเห็นข้อมูล real-time:**
```
data: {"type":"init","data":{"status":"running",...}}

data: {"type":"log","data":{"level":"info","message":"Starting place 1/1..."}}

data: {"type":"progress","data":{"current_page":1,"reviews_scraped":20}}

data: {"type":"progress","data":{"current_page":2,"reviews_scraped":40}}

data: {"type":"complete","data":{"result":{"total_reviews":100}}}
```

---

### ขั้นตอนที่ 5: ดาวน์โหลดผลลัพธ์

**ตำแหน่งไฟล์:**
```
outputs/20251110_235959_abc12345/
├── reviews.json          # ข้อมูลรีวิวทั้งหมด (JSON)
├── reviews.csv           # ข้อมูลรีวิวทั้งหมด (CSV)
└── metadata.json         # ข้อมูลสถิติและการตั้งค่า
```

**เปิดไฟล์:**
```bash
# Windows
start outputs\20251110_235959_abc12345\reviews.csv

# Mac
open outputs/20251110_235959_abc12345/reviews.csv

# Linux
xdg-open outputs/20251110_235959_abc12345/reviews.csv
```

---

## 📊 ตัวอย่างผลลัพธ์

### reviews.json
```json
[
  {
    "review_id": "ChZDSUhNMG9nS0VJQ0FnSURsMHVyYU5REAE",
    "author_name": "สมชาย ใจดี",
    "author_url": "https://www.google.com/maps/contrib/...",
    "author_reviews_count": 156,
    "rating": 5,
    "date_formatted": "15/10/2024",
    "date_relative": "1 เดือนที่แล้ว",
    "review_text": "อร่อยมาก! แนะนำเลยครับ",
    "review_likes": 12,
    "review_photos_count": 3,
    "owner_response": "ขอบคุณมากครับ",
    "page_number": 1,
    "place_id": "0x30da3a9a7e41e7e3:0x9ecd2c1918c6d746",
    "place_name": "ข้าวซอยนิมมาน"
  }
]
```

### metadata.json
```json
{
  "task_id": "20251110_235959_abc12345",
  "total_reviews": 100,
  "total_places": 1,
  "time_elapsed": 3.5,
  "scraping_rate": 28.57,
  "settings": {
    "max_reviews": 100,
    "language": "th",
    "date_range": "1year"
  },
  "final_progress": {
    "successful_requests": 5,
    "failed_requests": 0,
    "rate_limits_encountered": 0
  }
}
```

---

## 🎛️ การตั้งค่าขั้นสูง

### 1. ปรับความเร็ว

**Fast Mode (แนะนำ):**
```json
{
  "settings": {
    "fast_mode": true,
    "max_reviews": 1000
  }
}
```
- ความเร็ว: 26-30 รีวิว/วินาที
- ความเสี่ยง: ต่ำ (มีการป้องกัน anti-bot)

**Safe Mode (ปลอดภัยสูงสุด):**
```json
{
  "settings": {
    "fast_mode": false,
    "max_reviews": 1000
  }
}
```
- ความเร็ว: ~10 รีวิว/วินาที
- ความเสี่ยง: น้อยมาก

### 2. กรองตามวันที่

```json
{
  "settings": {
    "date_range": "1month"    // 1 เดือนล่าสุด
  }
}
```

**ตัวเลือก:**
- `"1month"` - 1 เดือน
- `"6months"` - 6 เดือน
- `"1year"` - 1 ปี (แนะนำ)
- `"5years"` - 5 ปี
- `"7years"` - 7 ปี
- `"all"` - ทั้งหมด

### 3. หลายภาษา

**ภาษาไทย:**
```json
{
  "settings": {
    "language": "th",
    "region": "th"
  }
}
```

**ภาษาอังกฤษ:**
```json
{
  "settings": {
    "language": "en",
    "region": "us"
  }
}
```

**ภาษาอื่นๆ:**
- `"ja"` + `"jp"` - ญี่ปุ่น
- `"zh-CN"` + `"cn"` - จีน
- `"ko"` + `"kr"` - เกาหลี

### 4. ดึงหลายสถานที่พร้อมกัน

```json
{
  "places": [
    {"place_id": "...", "name": "ร้านที่ 1"},
    {"place_id": "...", "name": "ร้านที่ 2"},
    {"place_id": "...", "name": "ร้านที่ 3"}
  ],
  "settings": {
    "max_reviews": 500
  }
}
```

---

## 🔍 การแก้ไขปัญหา

### ปัญหา: Server ไม่ทำงาน

**ตรวจสอบ:**
```bash
# ดู process ที่ใช้ port 5001
netstat -ano | findstr 5001

# Kill process (Windows)
taskkill /PID [PID] /F

# เริ่มใหม่
python webapp/api_v2.py
```

### ปัญหา: ค้นหาไม่เจอสถานที่

**สาเหตุ:**
- คำค้นหาไม่ถูกต้อง
- ภาษา/region ไม่ตรง

**แก้ไข:**
```json
{
  "query": "ชื่อร้าน + เมือง",  // เพิ่มชื่อเมืองช่วย
  "language": "th",              // ใช้ภาษาที่ตรงกับร้าน
  "region": "th"                 // ใช้ region ที่ตรง
}
```

### ปัญหา: Scraping ช้า

**สาเหตุ:**
- `fast_mode: false`
- Network ช้า
- Rate limit

**แก้ไข:**
```json
{
  "settings": {
    "fast_mode": true,    // เปิด fast mode
    "max_reviews": 100    // ลดจำนวนถ้าต้องการเร็ว
  }
}
```

### ปัญหา: รีวิวไม่ครบ

**สาเหตุ:**
- Google API มีข้อจำกัด (โดยทั่วไป 1000-2000 รีวิว)
- Date range filter out บางรีวิว

**แก้ไข:**
```json
{
  "settings": {
    "date_range": "all",      // ดึงทั้งหมด
    "max_reviews": 10000      // ตั้งสูงๆ
  }
}
```

### ปัญหา: Thai characters แสดงผิด

**Windows:**
```cmd
chcp 65001
```

**แก้ไขถาวร:** Code มีการจัดการ encoding อยู่แล้ว

---

## 📈 ตัวอย่างการใช้งานจริง

### กรณีที่ 1: วิจัยร้านอาหาร

**เป้าหมาย:** ดึงรีวิวร้านอาหารในกรุงเทพ 5 ร้าน

```bash
# 1. ค้นหา
curl -X POST http://localhost:5001/api/v2/search \
  -H "Content-Type: application/json" \
  -d '{"query":"ร้านอาหารอร่อย กรุงเทพ","max_results":5}'

# 2. ดึงข้อมูล (บันทึก place_id ทั้ง 5 ไว้)
curl -X POST http://localhost:5001/api/v2/scrape \
  -H "Content-Type: application/json" \
  -d '{
    "places": [/* ใส่ 5 places */],
    "settings": {"max_reviews":500, "date_range":"1year"}
  }'

# 3. รอผลลัพธ์
# ระยะเวลา: ~50 วินาที (500 รีวิว/ร้าน × 5 ร้าน ÷ 30 รีวิว/วินาที)

# 4. วิเคราะห์ CSV
# ใช้ Excel, Python pandas, หรือ Google Sheets
```

### กรณีที่ 2: ติดตามคู่แข่ง

**เป้าหมาย:** ติดตามรีวิวล่าสุดของคู่แข่ง

```bash
curl -X POST http://localhost:5001/api/v2/scrape \
  -H "Content-Type: application/json" \
  -d '{
    "places": [{"place_id":"...","name":"คู่แข่ง"}],
    "settings": {
      "max_reviews": 100,
      "date_range": "1month",    # รีวิวเดือนล่าสุด
      "fast_mode": true
    }
  }'
```

### กรณีที่ 3: วิเคราะห์หลายภาษา

**เป้าหมาย:** ดึงรีวิวทั้งภาษาไทยและอังกฤษ

```bash
# ภาษาไทย
curl -X POST http://localhost:5001/api/v2/scrape \
  -d '{"places":[...],"settings":{"language":"th","region":"th"}}'

# ภาษาอังกฤษ (สถานที่เดียวกัน)
curl -X POST http://localhost:5001/api/v2/scrape \
  -d '{"places":[...],"settings":{"language":"en","region":"us"}}'

# เปรียบเทียบความคิดเห็นจากคนไทย vs ต่างชาติ
```

---

## 🎯 เคล็ดลับการใช้งาน

### ✅ DO (ควรทำ)

1. **ใช้ Fast Mode** - เร็วและปลอดภัย
2. **ตั้ง max_reviews สมเหตุผล** - 100-500 ต่อร้าน
3. **ใช้ Date Range Filter** - ลดเวลา scraping
4. **บันทึก task_id** - เพื่อติดตามภายหลัง
5. **Monitor SSE Stream** - ดูความคืบหน้า real-time

### ❌ DON'T (ไม่ควรทำ)

1. **อย่า scrape บ่อยเกินไป** - ให้เว้นระยะห่าง
2. **อย่า scrape ร้านเดียวซ้ำๆ** - ทุก 1-2 ชั่วโมง
3. **อย่า set max_reviews > 5000** - Google จำกัดอยู่แล้ว
4. **อย่า run multiple tasks พร้อมกัน** - รอให้เสร็จทีละ task
5. **อย่า ignore error logs** - ตรวจสอบเมื่อมีปัญหา

---

## 🚀 ทดสอบระบบ

### Test Script

```bash
cd webapp
python test_api_v2.py
```

**จะทดสอบ:**
- ✓ API Connection
- ✓ Search API
- ✓ Scrape API
- ✓ Task Status
- ✓ SSE Stream
- ✓ List Tasks

**ผลลัพธ์ที่คาดหวัง:**
```
[SUCCESS] ALL TESTS PASSED
Results: 6/6 tests passed
```

---

## 📞 การขอความช่วยเหลือ

### เอกสารเพิ่มเติม

1. **PERFECT_SYSTEM_DOCUMENTATION.md** - เอกสารระบบฉบับสมบูรณ์
2. **CLAUDE.md** - คู่มือโปรเจคทั้งหมด
3. **CRITICAL_MISSING_FEATURES.md** - เปรียบเทียบฟีเจอร์

### ติดต่อ

- **GitHub Issues:** [Report Bug]
- **Email:** support@example.com

---

## ✨ สรุป

ระบบนี้ให้คุณ:

✅ **ค้นหาสถานที่** ด้วย API ที่รวดเร็ว
✅ **ดึงข้อมูลรีวิว** แบบ real-time
✅ **ติดตามความคืบหน้า** แบบละเอียด
✅ **ได้ข้อมูลครบถ้วน** 12 ฟิลด์
✅ **ปลอดภัย** ด้วย anti-bot protection
✅ **รวดเร็ว** 26-30 รีวิว/วินาที

**เริ่มต้นเลยตอนนี้!** 🚀

---

**เวอร์ชัน:** 1.0
**วันที่:** 2025-11-10
**สถานะ:** ✅ พร้อมใช้งาน

